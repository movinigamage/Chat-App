apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-chat
  namespace: chat-app
spec:
  podSelector:
    matchLabels:
      app: chat
  policyTypes:
    - Ingress
  ingress:
    - ports:
        - protocol: TCP
          port: 3000
